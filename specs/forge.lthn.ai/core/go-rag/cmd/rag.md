# rag
**Import:** `forge.lthn.ai/core/go-rag/cmd/rag`
**Files:** 5

## Types

This package exports no types.

## Functions

### Package Functions

### `func AddRAGSubcommands(parent *cli.Command)`
Initializes the package flag set through `initFlags`, adds the package's `ingest`, `query`, and `collections` commands to the shared `rag` command, and registers that `rag` command under `parent`.

### Methods

This package exports no methods.
