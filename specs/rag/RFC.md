# rag
**Import:** `forge.lthn.ai/core/go-rag/cmd/rag`
**Files:** 5

## Types

This package exports no types.

## Functions

### Package Functions

### `func AddRAGSubcommands(parent *cli.Command)`
Initialises the package flags through `initFlags`, adds `ingestCmd`, `queryCmd`, and `collectionsCmd` to the shared `ragCmd`, and registers `ragCmd` under `parent`.

### Methods

This package exports no methods.
